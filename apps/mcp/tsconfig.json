{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // DOM lib only because @orim/convert's palette import pulls in
    // @orim/renderer's types; no DOM API is executed at runtime here.
    "lib": ["ES2022", "DOM"],
    "types": ["node"]
  },
  "include": ["src"]
}
